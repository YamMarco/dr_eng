/** The word-card picture: one fixed 16:9 frame (`aspect-video` in WordImage),
 *  so the editor's cropper exports exactly what the player shows. */
export const WORD_IMAGE = { aspect: 16 / 9, width: 800, height: 450 } as const;
